function loadFooter() {
	loadFooterGeneric('#', '#', '#', '#', '#', '#', '#');
}

function loadHeader() {
	loadHeaderGeneric('<img src="${logo}" alt="${altBanner}" />', '${urlHome}');
}

function getSid() {
	return "${sid}";
}

function getPortalContext() {
	return "${context}";
}

function getContextId() {
	return "${context}";
}